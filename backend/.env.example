APP_NAME=ai-revenue-recovery-agent
ENV=local

# For local (non-Docker) dev, Postgres runs on localhost.
# When running via docker compose, this is overridden to point at the `db` service.
DATABASE_URL=postgresql+asyncpg://recovery_user:recovery_pass@localhost:5432/recovery_db
